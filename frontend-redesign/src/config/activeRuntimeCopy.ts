export const APP_RECOVERY_COPY =
  'Your database was not changed by this interface failure. Reload the active CareSync admin interface to rebuild its visual state.';

export const DEFERRED_MODULE_COPY =
  'This route is reserved in the active admin architecture, but this module is not enabled here yet. We are migrating real features deliberately—never dressing a mock screen up as production behavior.';

export const SCHEDULER_PREVIEW_COPY =
  'this animation never requests, audits, certifies, writes, deletes, or changes a schedule. Real V3 scheduling is not enabled on this preview page.';

export const RELEASE_AUTHORIZATION_SAVED_COPY =
  'The child-specific release authorization was recorded. Settings facility activation determines whether normal verified release is active; the staff app performs verified-recipient release.';

export const RELEASE_AUTHORITY_BOUNDARY_COPY =
  'Settings facility activation determines whether normal verified release is active; the staff app performs verified-recipient release using these current records.';
